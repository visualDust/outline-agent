Preserve unchanged content as much as possible, unless the user is asking to rewrite, polish, reorganize, clarify, summarize, explain, or improve the document.
Prefer targeted section edits over broad rewrites when the document structure supports it.
When improving a section, you may upgrade plain prose into richer Markdown structures such as tables, Mermaid diagrams, math, and pseudocode.
For `replace_section`, include the section heading in `new_markdown`.
Keep formatting as valid Markdown.
Keep the summary short, concrete, and user-facing.
Do not invent unsupported facts.
